# 11. 스타일 적용하기

<aside>
💡 스타일을 적용하는 방식은 크게 CSS, CSS-in-CSS(Sass 등), CSS-in-JS(styled-components 등)으로 나뉩니다. 프로젝트에 따라, 참여하는 인력에 따라 방식을 논의해보시면 좋습니다.

</aside>

# 1. CSS Modules

- 기본 세팅 코드
    
    ```jsx
    import React from "react";
    import Question from "./Components/Question";
    
    const App = () => {
      return (
        <>
           <nav className="box">
            <ul>
              <li id="detail" className="text">
                상세정보
              </li>
              <li id="qa" className="text">
                Q&A
              </li>
              <li id="review" className="text">
                Review
              </li>
            </ul>
          </nav>
          <Question />
        </>
      );
    };
    
    export default App;
    ```
    
    ```jsx
    import React from "react";
    
    const Question = () => {
      return (
        <div className="box">
          <h2 className="text">Q&A</h2>
          <p>
            Lorem ipsum dolor sit amet consectetur adipisicing elit. Eos excepturi
            corrupti quo blanditiis! Adipisci amet corporis ipsum odio minima
            aliquid quisquam! Dignissimos natus laborum qui veritatis quaerat eaque!
            Nemo, ullam.
          </p>
        </div>
      );
    };
    
    export default Question;
    ```
    

일반적인 상황에서 처음 `create-react-app`을 설치하면 볼 수 있는 CSS 파일과 JS파일입니다.  

`main.jsx`에 `main.css`를 import 하게 되면 전역으로 CSS가 적용됩니다. `App.jsx`에 `App.css`를 import하게 되면 App 컴포넌트에 들어있는 모든 컴포넌트에 적용됩니다.

![Untitled](Untitled%2021.png)

부모 컴포넌트에서 import한 css는 자식까지 자동으로 적용되고, 자식 컴포넌트에 새로 css를 적용하게 되면 **부모의 스타일은 import 순서에 따라 자식 컴포넌트에 import된 스타일이 덮어쓰게될 수 있습니다.** 

**따라서 리액트에서도 기본적으로 CSS 선택자 우선순위가 적용되며, 컴포넌트에 스타일을 사용하는게 결국 다른 컴포넌트의 스타일에도 영향을 미친다는것을 알 수 있습니다.**

```css
div {
  margin: 40px;
  background: blanchedalmond;
}
h2 {
  width: 200px;
  margin: 0 auto;
  text-align: center;
  color: Red;
}
```

```jsx
import "./App.css";
```

```css
div {
  background: pink;
}
h2 {
  width: 200px;
  margin: 0 auto;
  text-align: center;
  color: lemonchiffon;
}
```

```jsx
import "./Question.css";
```

결과 : 

![Untitled](Untitled%2022.png)

그러면 태그에 클래스 이름을 넣어주고 CSS에서 클래스 이름을 선택한 뒤 스타일을 적용하게 되면 어떻게 될까요?

### 1.1 일반 CSS일 때

`App.js`에 일반 css를 import하였습니다. 아래와 같이 스타일이 잘 적용되서 나옵니다.

```css
.box {
  margin: 40px;
  background: blanchedalmond;
}
.text {
  width: 200px;
  margin: 0 auto;
  text-align: center;
  color: Red;
}
```

```jsx
import "./App.css";
```

![Untitled](Untitled%2023.png)

이번에는 스타일을 `Question.jsx`에 import해보겠습니다. 

```css
.box {
  background: pink;
}
.text {
  width: 200px;
  margin: 0 auto;
  text-align: center;
  color: lemonchiffon;
}
```

```jsx
import "./Question.css";
```

자식 컴포넌트에서 import한 CSS가 적용된 것을 볼 수 있습니다. 클래스를 이용해 우선순위를 높이려는 노력도 결국 컴포넌트끼리 클래스 이름이 중복되면 소용없어지게 됩니다. 특히나 컴포넌트로 분리하여 작업을 하는 리액트의 특성상 클래스 이름이 겹칠 위험성은 상당히 커지게 됩니다.

이러한 문제를 해결하기 위해 CSS 파일 확장자를 .css에서 **module.css**로 바꿔줍니다.

<aside>
💡 `module.css`를 사용하면 class명에 고유값을 추가해주어서 겹치지 않도록 합니다.

</aside>

### 1.2 module.css일 때

`Question.css` 파일명을 `Question.module.css`로 바꿔봅시다.

`Question.jsx`에는 `{styles.클래스명}` 으로 바꿔주겠습니다.

```jsx
import React from "react";
import styles from "./Question.module.css";

const Question = () => {
  return (
    <div className={styles.box}>
      <h2 className={styles.text}>Q&A</h2>
      <p>
        Lorem ipsum dolor sit amet consectetur adipisicing elit. Eos excepturi
        corrupti quo blanditiis! Adipisci amet corporis ipsum odio minima
        aliquid quisquam! Dignissimos natus laborum qui veritatis quaerat eaque!
        Nemo, ullam.
      </p>
    </div>
  );
};

export default Question;
```

![Untitled](Untitled%2024.png)

App.css와 App.module.css도 바꿔줄게요

```jsx
import React from "react";
import styles from "./App.module.css";
import Question from "./Components/Question";

const App = () => {
  return (
    <>
      <nav className={styles.box}>
        <ul>
          <li id="detail" className={styles.text}>
            상세정보
          </li>
          <li id="qa" className={styles.text}>
            Q&A
          </li>
          <li id="review" className={styles.text}>
            Review
          </li>
        </ul>
      </nav>
      <Question />
    </>
  );
};

export default App;
```

![Untitled](Untitled%2024.png)

클래스 이름이 컴포넌트 별로 겹치지 않도록 클래스 명을 고민할 필요 없이 module을 파일명에 추가해준뒤 `styles.클래스명` 으로 해준다면 아래처럼 자동으로 클래스명이 겹치지 않도록 처리해줍니다.

- nav와 div 모두 box라는 style을 받고 있지만 이름이 다릅니다.

![Untitled](Untitled%2025.png)

module.css를 사용할 때 주의할 점은 적용하고자 하는 **컴포넌트 이름**과 파일명을 **반드시 일치**시켜줘야 합니다. 예를 들어 **Detail.jsx 파일에만 적용**해주고 싶은 module.css의 파일명은 **Detail.module.css**여야합니다.

<aside>
💡 module에서 태그로 지정해주고 싶을 때에는 아래와 같은 양식을 사용합니다.

```css
.main h1 {
    font-size: 40px;
}
```

```html
<div className={styles.main}>
    <h1>hello world</h1>
    <p className={styles.contents}>hello world</p>
<div/>
```

</aside>

결론적으로 CSS Modules는 기존 CSS 워크플로우와 유사하면서도 모듈화의 이점을 제공합니다.

# 2. styled-components

`styled-components`는 `CSS in JS` 관련하여 가장 인기 있는 리엑트 라이브러리입니다.

**styled-components 설치하기**

```jsx

npm install styled-components
// npm install styled-components@5
```

<aside>
💡 **참고하면 좋은 글**
[https://www.samsungsds.com/kr/insights/web_component.html](https://www.samsungsds.com/kr/insights/web_component.html)
[https://react.vlpt.us/styling/03-styled-components.html](https://react.vlpt.us/styling/03-styled-components.html)

</aside>

## 1.1 글로벌 스타일 만들기

글로벌 스타일은 `styled-components`가 제공하는 `createGlobalStyle()` 함수를 사용합니다. 

글로벌(전역) 스타일은 모든 컴포넌트에서 공통으로 적용되는 스타일을 말합니다. 예를 들어, 웹 페이지의 배경색이나 글꼴 설정 등은 모든 컴포넌트에서 공통으로 사용되는 스타일이므로 전역 스타일로 관리하는 것이 효율적입니다.

**`createGlobalStyle`** 함수를 사용하면 CSS 파일을 작성하지 않고도 전역 스타일을 손쉽게 생성할 수 있습니다.

먼저 `createGlobalStyle()` 함수를 사용하여 전역 스타일 컴포넌트를 생성합니다.  `App.js`(현재 최상위 컴포넌트) 상단에 추가해주면 모든 하위 컴포넌트에 스타일이 적용됩니다.

이 글로벌 스타일은 다른 스타일보다 나중에 로딩이 되어 같은 가중치라면 기본적으로 우선순위가 높다는 특징이 있습니다.

```jsx
import { createGlobalStyle } from "styled-components"
import Example from "./Components/Example"

const GlobalStyle = createGlobalStyle`
span {
  color: red;
  font-size: 12px;
}
`

function App() {
  return (
    <>
      <GlobalStyle/>
      <h1>hello world 1</h1>
      <span>hello world 2</span>
      <Example/>
    </>
  );
}

export default App;
```

```jsx
export default function Example() {
  return (
    <>
        <h1>hello world 3</h1>
        <span>hello world 4</span>
    </>
  )
}
```

styled-component 문법을 자세히 살펴보면 스타일을 꾸며줄 때 tagged template literal 문법을 사용하고 있다는걸 알 수 있습니다. 

```jsx
createGlobalStyle`
span {
  color: red;
  font-size: 12px;
}
`
```

문자열 리터럴을 함수 호출의 인자로 전달하여 해당 문자열을 가공하는 기능입니다. 이때 함수는 문자열 리터럴을 `$`표현식을 기준으로 나누어 각 부분을 배열의 요소로 전달받아, 이를 이용해 최종적인 문자열을 반환합니다. 이는 ES6 문법입니다. 아래 내용을 참고해주세요.

```jsx
const name = '이호준'
const age = 10

function 인사(문구, 이름, 나이){
    // console.log(문구)
    console.log(문구, 이름, 나이)
    return `${문구[0]}${이름}${문구[1]}${나이+나이}${문구[2]}`
}

const 인사문구 = 인사`이름은 ${name}이고 나이는 ${age+age}입니다.`

console.log(인사문구)
```

[Template literals - JavaScript | MDN](https://developer.mozilla.org/ko/docs/Web/JavaScript/Reference/Template_literals)

## 1.2 Reset css ([https://www.npmjs.com/package/styled-reset](https://www.npmjs.com/package/styled-reset))

Reset css는 styled-reset이라는 패키지를 다운받은 뒤 styled-reset이 제공하는 reset을 아래와 같이 글로벌 CSS에다가 적용합니다. 물론 단독으로 사용하셔도 무방합니다.

아래 링크처럼 normalize 코드도 제공하고 있습니다.

[npm: styled-normalize](https://www.npmjs.com/package/styled-normalize)

**styled-reset 설치하기**

```bash
npm i styled-reset
```

**글로벌 스타일에 styled-reset 적용하기**

```jsx
import { createGlobalStyle } from "styled-components"
import Example from "./Components/Example"
import reset from "styled-reset"

const GlobalStyle = createGlobalStyle`
    ${reset}
    
    span {
    color: red;
    font-size: 12px;
    }

    a{
        text-decoration : none;
        color : inherit;
    }

    button{
        border : none;
        cursor : pointer;
    }

    * {
    box-sizing: border-box;
    }
`

function App() {
  return (
    <>
      <GlobalStyle/>
      <h1>hello world 1</h1>
      <span>hello world 2</span>
      <Example/>
    </>
  );
}

export default App;
```

```jsx
export default function Example() {
  return (
    <>
        <h1>hello world 3</h1>
        <span>hello world 4</span>
    </>
  )
}
```

## 1.3 styled-components를 사용한 스타일 적용

`styled-components`로 스타일을 적용할 때는 `styled-components`에서 제공하는 styled를 사용합니다.

리액트의 인라인으로 스타일을 적용할 때와 다르게 기존 CSS의 온전한 문법 - 케밥 표기법(하이픈 표기법) - 으로 프로퍼티를 작성하면 됩니다.

```jsx
const 변수명 = styled.태그명`
	background-color : red;
`
```

```jsx
import React from "react";
import styled from "styled-components";

const ContentDiv = styled.div`
  margin: 40px;
`;

const ContentH2 = styled.h2`
  width: 200px;
  margin: 0 auto;
  text-align: center;
`;

const App = () => {
	return (
		<ContentDiv>
            <ContentH2>Q&A</ContentH2>
            <p>
                Lorem ipsum dolor sit amet consectetur adipisicing elit. Eos excepturi
                corrupti quo blanditiis! Adipisci amet corporis ipsum odio minima
                aliquid quisquam! Dignissimos natus laborum qui veritatis quaerat eaque!
                Nemo, ullam.
            </p>
        </ContentDiv>
	);
};

export default App;
```

![Untitled](Untitled%2026.png)

## 1.4 props에 따른 조건부 스타일

props를 사용해서 조건에 따라 스타일을 적용해줄 수 있습니다. 문자열 리터럴 안에 자바스크립트 코드를 넣을 때 $를 사용하듯이 `${}` 안에 코드를 입력해주면 됩니다. 

```jsx
import React from "react";
import styled from "styled-components";

const ContentDiv = styled.div`
    margin: 40px;
`;

const ContentH2 = styled.h2`
    color: ${(props) => (props.name === 'hello'? 'red' : 'black')};
    width: 200px;
    margin: 0 auto;
    text-align: center;
`;

const App = () => {
    return (
        <ContentDiv>
            <ContentH2 name="hello">Q&A</ContentH2>
            <p>
                Lorem ipsum dolor sit amet consectetur adipisicing elit. Eos excepturi
                corrupti quo blanditiis! Adipisci amet corporis ipsum odio minima
                aliquid quisquam! Dignissimos natus laborum qui veritatis quaerat eaque!
                Nemo, ullam.
            </p>
        </ContentDiv>
    );
};

export default App;
```

![Untitled](Untitled%2027.png)

아래처럼 확장해서 사용할 수 있습니다.

```jsx
import React from "react";
import styled from "styled-components";

const ContentOne = styled.div`
    margin: 40px;
`;

const ContentTwo = styled.div`
    color:red;
`;

const ContentThree = styled(ContentTwo)`
    border: 1px solid black;
`

function App() {
    return (
        <div>
            <ContentOne>hello world</ContentOne>
            <ContentTwo>hello world</ContentTwo>
            <ContentThree>hello world</ContentThree>
        </div>
    );
}

export default App;
```

## 1.5 다양한 문법 활용

아래와 같이 Sass문법을 사용할 수도 있습니다.

```sass
a {
  /* style 1 */
  &::after {
    /* style 2 */
  }
}

// 컴파일 후
// a { /* style 1 */ }
// a::after { /* style 2 */ }
```

```jsx
const UniAfter = styled.div`
    &:after {
    content: "!!";
}`
```

또한 아래와 같이 CSS 확장이 가능합니다.

```jsx
import React from "react";
import styled, { css } from 'styled-components'

const One = css`
  color: red;
`;

const Two = css`
  border: 1px solid black;
`;

const Three = styled.div`
  ${One}
  ${Two}
`

const App = () => {
	return (
    <Three>Lorem ipsum dolor</Three>
	);
};
  
export default App;
```

실습 - styled-components 를 이용해 다양한 스타일의 버튼 3가지를 만들어보세요. 그리고 이 중 두개의 버튼은 첫번째 버튼의 스타일을 확장하여 만들어봅시다!

![스크린샷 2023-05-11 오후 11.51.58.png](%25E1%2584%2589%25E1%2585%25B3%25E1%2584%258F%25E1%2585%25B3%25E1%2584%2585%25E1%2585%25B5%25E1%2586%25AB%25E1%2584%2589%25E1%2585%25A3%25E1%2586%25BA_2023-05-11_%25E1%2584%258B%25E1%2585%25A9%25E1%2584%2592%25E1%2585%25AE_11.51.58.png)

실습2 : 
아래와 같은 컴포넌트가 있습니다.

```markdown
<StyledButton color="danger" size="large" fullWidth >hello</StyledButton>
```

danger 는 붉은 배경색을 의미하고, size 는 버튼에 마우스를 올렸을 때 커지는 크기를 의미합니다. size의 값으로 ‘large’ 가 전달되면 1.5배 커지게 됩니다.
fullWidth 의 값이 true 라면 버튼의 넓이는 100% 가 됩니다.
이와 같은 컴포넌트를 만들어보세요.

<aside>
🤔 **Transient props**

임시 속성

만약 style을 위한 props 가 너무 많아져서 최종적으로 돔 트리에 랜더링 될 props가 불필요하게 너무 많다면 임시 속성으로 만들어 props의 랜더링을 막을 수 있습니다. 이는 styled-components 만의 고유한 기능이며, props의 이름 앞에 $를 붙이면 지정됩니다.

그리고 이와 같은 방법은  react 가 명확히 boolean 속성값을 가지는 속성으로 인식하지 못하는 사용자 정의 속성의 경우도 true, false 와 같은 boolean 값을 가지도록 하는 수단이 될 수 있습니다.

```jsx
<StyledProps size="large" $fullwidth={false}>버튼입니다.</StyledProps>
```

위의 `fullwidth`는 앞에 $가 없다면 boolean 값 대신 문자열(`’false’`)을 이용해야합니다.

</aside>