수정한 것을 보니까 챕터마다 `# 1. 제목` 이렇게 시작해야 하는데 `# 1.3` 등으로 소제목으로 시작하는 부분이 있어. 각 `_md` 폴더에 있는 모든 `.md` 파일은 챕터별로 `# 1. 제목` 이런 형식으로 시작하도록 고쳐줘.

그리고 내용을 읽어가면서 `<aside>`, `💡`, `</aside>`로 되어 있는 부분은 지우고 `:::div{.callout}`, `내용`, `:::`로 고쳐줘.