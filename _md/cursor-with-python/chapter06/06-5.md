---
chapter: Python 심화 개념 배우기 - "함수와 클래스의 첫걸음"
title: Rules & Memories 설정 - "AI에게 내 스타일 가르치기"
date: 2025-01-21
---

# Rules & Memories 설정 - "AI에게 내 스타일 가르치기"

## 🎯 이번 시간에 배울 것

- **Rules 시스템으로 코딩 스타일 자동화하기**
- **Memories 기능으로 프로젝트 컨텍스트 관리하기**
- **AI가 내 방식을 기억하고 따라하게 만들기**
- **개발 효율성을 극대화하는 고급 설정법**

## 🌟 Chapter 6-1~6-4에서 배운 것

### 지금까지의 성과

**6-1~6-3: Python 심화 개념** ✅
- 함수로 코드 정리하기
- 클래스로 객체 만들기  
- 모듈과 패키지 활용하기

**6-4: 종합 실습** ✅
- 체계적인 프로그램 구조 만들기

**이제 Chapter 6-5에서는...** 🚀
- **이제 Python 기초를 마스터했으니, AI가 나만의 개발 스타일을 학습하고 자동으로 적용하게 만들어보겠습니다!**

## 🎯 Rules 시스템 이해하기

### 🤖 AI에게 규칙 가르치기

**Rules = AI에게 주는 개발 가이드라인**

**😰 기존 방식의 문제점:**
- 매번 같은 스타일 요청을 반복
- AI가 내 선호도를 기억하지 못함  
- 일관성 없는 코드 생성
- 매 프로젝트마다 처음부터 설명

**✨ Rules 시스템의 장점:**
- 한 번 설정하면 항상 적용
- 내 코딩 스타일을 AI가 학습
- 일관된 코드 품질 유지
- 팀 전체 코딩 표준 적용 가능

### 📝 초심자용 Rules 작성 예시

**Cursor에서 Rules 설정하기:**

1. **Cursor 설정** → **Rules** 섹션
2. **새 Rule 추가** 클릭
3. **Rule 내용 작성**

**예시 1: 초심자를 위한 Python 코딩 스타일**

```markdown
# 초심자용 Python 코딩 가이드

## 함수 작성 규칙
- 모든 함수에 한국어로 설명 주석 작성
- 함수명은 동사_명사 형태로 (예: calculate_average)
- 한 함수는 하나의 일만 하기
- 매개변수는 최대 3개까지만 사용

## 변수명 규칙  
- 변수명은 의미를 알 수 있게 명확히 작성
- 한글 주석으로 변수 용도 설명
- 숫자로 시작하지 않기
- 예약어 사용하지 않기

## 코드 구조
- 한 줄에 한 가지 일만 하기
- 복잡한 계산은 여러 줄로 나누기
- if문, for문 등은 들여쓰기 정확히 하기
- print문으로 중간 결과 확인하기

## 주석 스타일
- 어려운 부분에는 반드시 설명 추가
- "왜 이렇게 했는지" 이유도 설명
- 다른 사람이 봐도 이해할 수 있게 작성
```

**예시 2: 클래스 사용 가이드**

```markdown
# 클래스 설계 가이드 (초심자용)

## 클래스 기본 원칙
- 관련된 데이터와 기능을 하나로 묶기
- __init__에서 모든 속성 초기화
- 메소드 이름은 동작을 명확히 표현
- 클래스명은 명사로 시작 (예: Student, Car)

## 간단한 구조 유지
- 한 클래스에 메소드 10개 이하
- 상속은 꼭 필요할 때만 사용
- 복잡한 기능보다는 단순하고 명확하게
- 속성은 외부에서 직접 접근 가능하게

## 실용적인 설계
- 실제 사용할 기능 위주로 구현
- 완벽보다는 동작하는 코드 우선
- 나중에 필요하면 기능 추가하기
```

## 💾 Memories 시스템 활용하기

### 🧠 프로젝트 컨텍스트 관리

**Memories = AI가 프로젝트에 대해 기억하는 정보**

### 초심자도 쉽게 사용하는 Memories 활용법

**1. 프로젝트 기본 정보 간단히 저장**

```markdown
# 내 첫 Python 프로젝트

## 프로젝트 소개
- 이름: 학급 성적 관리 프로그램
- 목적: 우리 반 학생들 성적 계산하기
- 사용자: 나 (고등학생)

## 사용하는 기술
- Python 기초 (변수, 리스트, 함수)
- 간단한 클래스 (Student 클래스)
- 파일 저장은 나중에 배우면 추가

## 내가 배운 것들
- 함수로 반복 작업 줄이기
- 클래스로 학생 정보 정리하기
- 평균 계산하는 방법
```

**2. 개발하면서 배운 점들 기록**

```markdown
# 개발 일기

## 2025-01-21: 함수 사용법 이해
- 같은 계산을 여러 번 할 때 함수 사용
- def 키워드로 함수 만들기
- return으로 결과 돌려주기
- 함수 이름은 하는 일을 명확히 표현

## 2025-01-21: 클래스 만들기 도전
- Student 클래스로 학생 정보 관리
- __init__으로 초기값 설정
- 메소드로 평균 계산 기능 추가
- 여러 학생 객체 만들어서 테스트

## 실수했던 것들
- 함수에서 return 빼먹기
- 클래스에서 self 안 쓰기
- 들여쓰기 잘못하기
```

**3. 앞으로 만들고 싶은 것들**

```markdown
# 프로젝트 아이디어

## 단기 목표 (이번 달)
- 성적 관리 프로그램 완성하기
- 파일에 저장하는 기능 추가
- 그래프로 성적 보여주기

## 장기 목표 (3개월 후)
- 간단한 게임 만들기 (가위바위보)
- 웹사이트 만들어보기
- 데이터베이스 사용해보기

## 관심 있는 분야
- 게임 개발
- 웹 개발
- 인공지능
```

## 🚀 실습: Rules & Memories 적용하기

### 실제 프로젝트에 적용해보기

**Chat에서 Rules와 Memories를 활용한 대화:**

```
@rules @memories

지금까지 배운 함수와 클래스를 사용해서 
간단한 도서 관리 프로그램을 만들고 싶습니다.

기능:
- 책 정보 저장 (제목, 저자, 페이지 수)
- 책 목록 보기
- 책 검색하기
- 읽은 페이지 기록하기

초심자 수준에 맞게, 우리가 배운 방식으로 만들어주세요.
```

**AI가 Rules와 Memories를 반영한 답변:**

```python
# 도서 관리 프로그램
# 초심자가 이해하기 쉽게 단순하게 구현

class Book:
    """책 정보를 관리하는 클래스"""
    
    def __init__(self, title, author, total_pages):
        """책 객체 만들기"""
        self.title = title          # 책 제목
        self.author = author        # 저자
        self.total_pages = total_pages  # 전체 페이지
        self.read_pages = 0         # 읽은 페이지 (처음엔 0)
    
    def read_book(self, pages):
        """책 읽기 - 읽은 페이지 추가"""
        self.read_pages += pages
        
        # 전체 페이지보다 많이 읽을 수 없음
        if self.read_pages > self.total_pages:
            self.read_pages = self.total_pages
        
        print(f"📖 {pages}페이지를 읽었습니다!")
        print(f"   현재 진행률: {self.read_pages}/{self.total_pages}페이지")
    
    def show_info(self):
        """책 정보 보여주기"""
        progress = (self.read_pages / self.total_pages) * 100
        print(f"📚 제목: {self.title}")
        print(f"   저자: {self.author}")
        print(f"   진행률: {self.read_pages}/{self.total_pages}페이지 ({progress:.1f}%)")
        
        # 완독 여부 확인
        if self.read_pages == self.total_pages:
            print("   🎉 완독 완료!")
        else:
            remaining = self.total_pages - self.read_pages
            print(f"   📖 남은 페이지: {remaining}페이지")

class BookManager:
    """여러 책을 관리하는 클래스"""
    
    def __init__(self):
        """도서 관리자 만들기"""
        self.books = []  # 책들을 저장할 리스트
    
    def add_book(self, title, author, total_pages):
        """새 책 추가하기"""
        new_book = Book(title, author, total_pages)
        self.books.append(new_book)
        print(f"✅ '{title}' 책이 추가되었습니다!")
    
    def show_all_books(self):
        """모든 책 목록 보기"""
        if not self.books:  # 책이 없으면
            print("📚 등록된 책이 없습니다.")
            return
        
        print("📚 내 도서 목록:")
        print("-" * 40)
        for i, book in enumerate(self.books, 1):
            print(f"{i}. {book.title} - {book.author}")
            progress = (book.read_pages / book.total_pages) * 100
            print(f"   진행률: {progress:.1f}%")
        print("-" * 40)
    
    def find_book(self, search_word):
        """책 찾기 (제목에서 검색)"""
        found_books = []
        
        for book in self.books:
            # 제목에 검색어가 포함되어 있으면
            if search_word in book.title:
                found_books.append(book)
        
        if found_books:
            print(f"🔍 '{search_word}' 검색 결과:")
            for book in found_books:
                book.show_info()
                print()
        else:
            print(f"❌ '{search_word}'가 포함된 책을 찾을 수 없습니다.")
    
    def read_book_by_number(self, book_number, pages):
        """번호로 책 선택해서 읽기"""
        if 1 <= book_number <= len(self.books):
            book = self.books[book_number - 1]
            book.read_book(pages)
        else:
            print("❌ 잘못된 책 번호입니다.")

# 사용 예시
def main():
    """메인 프로그램"""
    # 도서 관리자 만들기
    manager = BookManager()
    
    # 몇 권의 책 추가해보기
    manager.add_book("어린왕자", "생텍쥐페리", 200)
    manager.add_book("해리포터", "J.K.롤링", 500)
    manager.add_book("파이썬 첫걸음", "홍길동", 300)
    
    # 전체 목록 보기
    manager.show_all_books()
    
    # 책 읽기
    manager.read_book_by_number(1, 50)  # 1번 책 50페이지 읽기
    manager.read_book_by_number(1, 100) # 1번 책 100페이지 더 읽기
    
    # 책 검색해보기
    manager.find_book("파이썬")

# 프로그램 실행
if __name__ == "__main__":
    main()
```

### Memory에 새로운 학습 내용 추가

**Chat에서:**

```
@memories

오늘 Rules와 Memories를 배우면서 깨달은 점들을 기록해주세요:

1. Rules는 AI에게 내 코딩 스타일을 가르치는 방법
2. Memories는 프로젝트 정보를 AI가 기억하게 하는 방법
3. 함수와 클래스를 배운 후에 이런 고급 기능을 사용하니 더 효과적
4. 간단한 도서 관리 프로그램을 만들어봤음

앞으로 이런 방식으로 프로젝트를 진행하면 좋을 것 같습니다.
```

## 🌟 Rules & Memories 활용 팁

### 📝 초심자를 위한 Rules 작성법

**1. 너무 복잡하게 만들지 말기**
```markdown
❌ 복잡한 예: "Design patterns, SOLID principles, performance optimization"
✅ 간단한 예: "함수는 짧게, 변수명은 명확하게, 주석은 자세히"
```

**2. 내가 자주 하는 실수 방지하기**
```markdown
# 내가 자주 하는 실수들
- return 빼먹지 않기
- 들여쓰기 정확히 하기
- 변수명 철자 확인하기
- 함수 호출할 때 괄호 잊지 않기
```

**3. 단계적으로 늘려가기**
```markdown
# 1단계: 기본 규칙 (지금 수준)
- 함수와 클래스 사용법

# 2단계: 중급 규칙 (1달 후)
- 파일 처리와 에러 처리

# 3단계: 고급 규칙 (3달 후)
- 라이브러리 사용과 최적화
```

### 🧠 효과적인 Memories 관리

**1. 정기적으로 업데이트하기**
- 새로운 기능 배울 때마다 기록
- 실수했던 부분도 기록해서 반복 방지
- 성공한 코드도 기록해서 재사용

**2. 카테고리별로 정리하기**
- 배운 개념들 (함수, 클래스, 모듈)
- 만든 프로젝트들
- 앞으로의 계획들

**3. 간단하고 이해하기 쉽게 작성하기**
- 전문 용어보다는 쉬운 말로
- 예시 코드와 함께 설명
- 왜 그렇게 했는지 이유도 함께

## 🎯 오늘의 과제

**Rules & Memories 마스터하기:**

1. **나만의 Rules 만들기:**
   - 지금까지 배운 것 중 중요한 규칙들 정리
   - 자주 하는 실수들 방지 규칙 추가
   - 앞으로 지키고 싶은 코딩 습관 작성

2. **프로젝트 Memories 구축:**
   - 지금까지 만든 프로그램들 정리
   - 각각에서 배운 점들 기록
   - 다음에 만들고 싶은 프로그램 아이디어

3. **실전 활용하기:**
   - @rules @memories와 함께 AI에게 질문하기
   - 새 프로젝트 시작할 때 활용하기
   - 친구들과 공유할 만한 좋은 규칙 찾기

**AI 활용 팁:**
- "@rules @memories"로 컨텍스트 활용
- "내 스타일에 맞게..."
- "이전에 배운 방식으로..."

## 💡 다음 시간 예고

다음 Chapter 6-6에서는 **Indexing & Docs 활용 - 프로젝트 문서화 마스터하기**를 배워보겠습니다!

**여러분은 이제 AI가 내 개발 스타일을 완벽히 이해하고 도와주는 개인 맞춤형 개발 환경을 구축한 개발자입니다!** 🚀