---
chapter: Python 심화 개념 배우기 - "함수와 클래스의 첫걸음"
title: 클래스 기초 - 객체 만들기
date: 2025-01-21
---

# 클래스 기초 - 객체 만들기

## 🎯 이번 시간에 배울 것

- **클래스가 무엇인지 쉽게 이해하기**
- **실생활 예시로 객체 개념 파악하기**
- **간단한 클래스를 만들고 사용해보기**

## 🌟 Chapter 6-1에서 배운 것

### 지난 시간 복습

Chapter 6-1에서 **함수**를 배웠죠!

```
✅ 반복되는 코드를 함수로 정리하기
✅ 매개변수와 반환값 사용하기
✅ 계산기, 게임 함수 만들기
```

**이제 Chapter 6-2에서는...** 🚀
- **클래스라는 더 강력한 코드 정리 방법**을 배워보겠습니다!

## 🤔 클래스가 무엇일까요?

### 🏠 실생활 예시로 이해하기

**상황: 학교에서 학생들 정보 관리**

**함수만 사용했을 때:**
```python
# 학생1 정보
student1_name = "김철수"
student1_age = 16
student1_grade = 1
student1_scores = [90, 85, 88]

# 학생2 정보
student2_name = "이영희"
student2_age = 17
student2_grade = 2
student2_scores = [92, 88, 95]

# 평균 계산 함수
def calculate_average(scores):
    return sum(scores) / len(scores)

# 학생1 평균
avg1 = calculate_average(student1_scores)
print(f"{student1_name}의 평균: {avg1}")

# 학생2 평균
avg2 = calculate_average(student2_scores)
print(f"{student2_name}의 평균: {avg2}")
```

**문제점:**
❌ 학생이 많아지면 변수가 너무 많아짐  
❌ 학생 정보가 흩어져 있어서 관리하기 어려움  
❌ 실수로 잘못된 정보를 연결할 수 있음

### ✨ 클래스로 해결하기

**클래스 = 붕어빵 틀**  
**객체 = 붕어빵**

```python
class Student:
    """학생 클래스 - 학생 정보를 한 곳에 정리"""
    
    def __init__(self, name, age, grade):
        """학생 객체를 만들 때 초기 정보 설정"""
        self.name = name
        self.age = age
        self.grade = grade
        self.scores = []
    
    def add_score(self, score):
        """점수 추가하기"""
        self.scores.append(score)
        print(f"{self.name}의 점수 {score}점이 추가되었습니다!")
    
    def calculate_average(self):
        """평균 계산하기"""
        if self.scores:
            return sum(self.scores) / len(self.scores)
        else:
            return 0
    
    def show_info(self):
        """학생 정보 보여주기"""
        avg = self.calculate_average()
        print(f"이름: {self.name}")
        print(f"나이: {self.age}세")
        print(f"학년: {self.grade}학년")
        print(f"점수: {self.scores}")
        print(f"평균: {avg:.1f}점")

# 학생 객체 만들기 (붕어빵 만들기)
student1 = Student("김철수", 16, 1)
student2 = Student("이영희", 17, 2)

# 점수 추가
student1.add_score(90)
student1.add_score(85)
student1.add_score(88)

student2.add_score(92)
student2.add_score(88)
student2.add_score(95)

# 정보 출력
student1.show_info()
print()
student2.show_info()
```

**장점:**
✅ 한 학생의 모든 정보가 하나로 묶여있음  
✅ 코드가 깔끔하고 이해하기 쉬움  
✅ 학생마다 같은 기능을 사용할 수 있음  
✅ 실수할 가능성이 줄어듦

## 🧠 클래스 구조 이해하기

### 📋 클래스의 기본 구조

```python
class 클래스명:
    """클래스 설명"""
    
    def __init__(self, 매개변수들):
        """생성자: 객체를 만들 때 실행되는 함수"""
        self.속성1 = 값1
        self.속성2 = 값2
    
    def 메소드명(self, 매개변수들):
        """메소드: 객체가 할 수 있는 행동"""
        # 할 일들
        return 결과
```

**구성 요소:**
- **class**: 클래스를 만든다는 키워드
- **__init__**: 객체를 만들 때 자동 실행되는 특별한 함수
- **self**: 자기 자신을 가리키는 특별한 변수
- **속성**: 객체가 가진 정보 (변수)
- **메소드**: 객체가 할 수 있는 행동 (함수)

## 🚀 실습 1: 간단한 클래스 만들기

### AI와 함께 강아지 클래스 만들기

**Chat에서:**

```
클래스를 처음 배우는 초심자입니다.

강아지 클래스를 만들어주세요:
- 강아지 이름, 나이, 품종 정보
- 짖기, 꼬리 흔들기, 정보 보여주기 기능

간단하고 이해하기 쉽게 만들어주시고,
클래스와 객체의 차이점도 설명해주세요.
```

### AI가 만들어준 강아지 클래스

```python
class Dog:
    """강아지 클래스"""
    
    def __init__(self, name, age, breed):
        """강아지 객체 생성할 때 정보 설정"""
        self.name = name        # 강아지 이름
        self.age = age          # 강아지 나이
        self.breed = breed      # 강아지 품종
        self.happiness = 50     # 행복도 (기본값 50)
    
    def bark(self):
        """짖기"""
        print(f"{self.name}: 멍멍! 🐕")
        self.happiness += 5
    
    def wag_tail(self):
        """꼬리 흔들기"""
        print(f"{self.name}이(가) 꼬리를 흔듭니다! 🐕💕")
        self.happiness += 10
    
    def play(self):
        """놀기"""
        print(f"{self.name}이(가) 신나게 놉니다! 🎾")
        self.happiness += 15
        if self.happiness > 100:
            self.happiness = 100
    
    def show_status(self):
        """강아지 상태 보여주기"""
        print(f"🐕 강아지 정보")
        print(f"이름: {self.name}")
        print(f"나이: {self.age}살")
        print(f"품종: {self.breed}")
        print(f"행복도: {self.happiness}/100")
        print("-" * 20)

# 강아지 객체들 만들기
dog1 = Dog("멍멍이", 3, "골든 리트리버")
dog2 = Dog("뽀삐", 2, "치와와")
dog3 = Dog("바둑이", 5, "진돗개")

# 강아지들과 놀아주기
dog1.show_status()
dog1.bark()
dog1.wag_tail()
dog1.play()
dog1.show_status()

print()

dog2.show_status()
dog2.wag_tail()
dog2.play()
dog2.show_status()
```

### 🎯 클래스 vs 객체 이해하기

**클래스 (Dog):**
- 강아지의 **설계도**
- "강아지는 이름, 나이, 품종을 가지고 있고, 짖을 수 있다"
- **한 번만 정의**

**객체 (dog1, dog2, dog3):**
- 설계도로 만든 **실제 강아지**
- "멍멍이(3살, 골든 리트리버)", "뽀삐(2살, 치와와)"
- **여러 개 만들 수 있음**

## 🎮 실습 2: 게임 캐릭터 클래스

### RPG 게임 캐릭터 만들기

**Chat에서:**

```
간단한 RPG 게임 캐릭터 클래스를 만들어주세요.

기능:
- 캐릭터 이름, 체력, 공격력, 레벨
- 공격하기, 체력 회복하기, 레벨업하기
- 캐릭터 상태 보여주기

초심자가 이해하기 쉽게 간단하게 만들어주세요.
```

### AI가 만들어준 캐릭터 클래스

```python
class Character:
    """RPG 게임 캐릭터 클래스"""
    
    def __init__(self, name, character_class="전사"):
        """캐릭터 생성"""
        self.name = name
        self.character_class = character_class
        self.level = 1
        self.health = 100
        self.max_health = 100
        self.attack_power = 20
        self.experience = 0
    
    def attack(self, target):
        """다른 캐릭터 공격하기"""
        damage = self.attack_power
        target.health -= damage
        if target.health < 0:
            target.health = 0
        
        print(f"⚔️ {self.name}이(가) {target.name}을(를) 공격!")
        print(f"   {damage} 데미지를 입혔습니다!")
        print(f"   {target.name}의 남은 체력: {target.health}")
        
        if target.health == 0:
            print(f"💀 {target.name}이(가) 쓰러졌습니다!")
            self.gain_experience(50)
    
    def heal(self):
        """체력 회복하기"""
        heal_amount = 30
        self.health += heal_amount
        if self.health > self.max_health:
            self.health = self.max_health
        
        print(f"💚 {self.name}이(가) 체력을 회복했습니다!")
        print(f"   현재 체력: {self.health}/{self.max_health}")
    
    def gain_experience(self, exp):
        """경험치 획득"""
        self.experience += exp
        print(f"✨ {self.name}이(가) 경험치 {exp}를 획득했습니다!")
        
        if self.experience >= 100:
            self.level_up()
    
    def level_up(self):
        """레벨업"""
        self.level += 1
        self.experience = 0
        self.max_health += 20
        self.health = self.max_health
        self.attack_power += 5
        
        print(f"🎉 {self.name}이(가) 레벨업했습니다!")
        print(f"   레벨 {self.level}이 되었습니다!")
    
    def show_status(self):
        """캐릭터 상태 표시"""
        print(f"📊 {self.name}의 상태")
        print(f"직업: {self.character_class}")
        print(f"레벨: {self.level}")
        print(f"체력: {self.health}/{self.max_health}")
        print(f"공격력: {self.attack_power}")
        print(f"경험치: {self.experience}/100")
        print("-" * 25)

# 캐릭터들 생성
hero = Character("용사", "전사")
monster = Character("고블린", "몬스터")

# 게임 시뮬레이션
hero.show_status()
monster.show_status()

print("\n🔥 전투 시작!")
hero.attack(monster)
hero.attack(monster)
monster.attack(hero)

print("\n💚 용사가 체력을 회복합니다.")
hero.heal()

print("\n⚔️ 마지막 공격!")
hero.attack(monster)

print("\n📊 최종 상태:")
hero.show_status()
```

## 🏪 실습 3: 실용적인 은행 계좌 클래스

### 간단한 은행 계좌 관리

**Chat에서:**

```
은행 계좌 클래스를 만들어주세요.

기능:
- 계좌 소유자 이름, 계좌번호, 잔액
- 입금하기, 출금하기, 잔액 조회하기
- 거래 내역 기록하기

실제로 사용할 수 있을 정도로 만들되,
초심자가 이해하기 쉽게 해주세요.
```

### AI가 만들어준 계좌 클래스

```python
class BankAccount:
    """은행 계좌 클래스"""
    
    def __init__(self, owner_name, account_number, initial_balance=0):
        """계좌 생성"""
        self.owner_name = owner_name
        self.account_number = account_number
        self.balance = initial_balance
        self.transaction_history = []
        
        # 계좌 개설 기록
        self.transaction_history.append(f"계좌 개설 (초기 잔액: {initial_balance:,}원)")
        print(f"🏦 {owner_name}님의 계좌가 개설되었습니다!")
        print(f"   계좌번호: {account_number}")
        print(f"   초기 잔액: {initial_balance:,}원")
    
    def deposit(self, amount):
        """입금하기"""
        if amount <= 0:
            print("❌ 0원보다 큰 금액을 입금해주세요!")
            return False
        
        self.balance += amount
        self.transaction_history.append(f"입금: +{amount:,}원 (잔액: {self.balance:,}원)")
        
        print(f"💰 입금 완료!")
        print(f"   입금액: {amount:,}원")
        print(f"   현재 잔액: {self.balance:,}원")
        return True
    
    def withdraw(self, amount):
        """출금하기"""
        if amount <= 0:
            print("❌ 0원보다 큰 금액을 출금해주세요!")
            return False
        
        if amount > self.balance:
            print("❌ 잔액이 부족합니다!")
            print(f"   현재 잔액: {self.balance:,}원")
            return False
        
        self.balance -= amount
        self.transaction_history.append(f"출금: -{amount:,}원 (잔액: {self.balance:,}원)")
        
        print(f"💸 출금 완료!")
        print(f"   출금액: {amount:,}원")
        print(f"   현재 잔액: {self.balance:,}원")
        return True
    
    def check_balance(self):
        """잔액 조회"""
        print(f"💳 {self.owner_name}님의 계좌 정보")
        print(f"   계좌번호: {self.account_number}")
        print(f"   현재 잔액: {self.balance:,}원")
        return self.balance
    
    def show_history(self):
        """거래 내역 보기"""
        print(f"📋 {self.owner_name}님의 거래 내역")
        print(f"계좌번호: {self.account_number}")
        print("-" * 40)
        
        for i, transaction in enumerate(self.transaction_history, 1):
            print(f"{i}. {transaction}")
        
        print("-" * 40)
        print(f"현재 잔액: {self.balance:,}원")

# 계좌 만들기
account1 = BankAccount("김철수", "123-456-789", 100000)
account2 = BankAccount("이영희", "987-654-321", 50000)

print("\n" + "="*50)

# 거래해보기
account1.deposit(50000)
account1.withdraw(30000)
account1.check_balance()

print("\n" + "="*50)

account2.deposit(20000)
account2.withdraw(80000)  # 잔액 부족
account2.withdraw(30000)
account2.show_history()
```

## 🌟 학습 성과 체크

### 오늘 배운 내용

```
✅ 클래스와 객체의 개념 이해
✅ __init__ 생성자 사용법
✅ 속성과 메소드 만들기
✅ 실용적인 클래스 설계하기
```

### 클래스의 장점

**Before (클래스 없이):**
- 관련된 정보가 흩어져 있음
- 같은 작업을 반복해서 코딩
- 코드가 복잡하고 관리하기 어려움

**After (클래스 활용):**
- 관련 정보와 기능이 하나로 묶임
- 객체를 여러 개 만들어 재사용
- 코드가 체계적이고 이해하기 쉬움

## 🎯 오늘의 과제

**클래스 마스터하기:**

1. **도서 클래스 만들기:**
   - 제목, 저자, 페이지 수, 현재 페이지
   - 책 읽기, 책갈피 저장, 정보 보기 기능

2. **자동차 클래스 만들기:**
   - 브랜드, 모델, 연료량, 속도
   - 시동 걸기, 가속, 브레이크, 주유 기능

3. **음식 주문 클래스 만들기:**
   - 음식명, 가격, 수량, 총액
   - 수량 변경, 총액 계산, 주문 확인 기능

**AI 활용 팁:**
- "이 클래스에 새로운 기능을 추가해주세요"
- "클래스를 더 간단하게 만들어주세요"
- "실제 사용 예시도 보여주세요"

## 💡 다음 시간 예고

다음 Chapter 6-3에서는 **모듈과 패키지 사용하기**를 배워보겠습니다!

**여러분은 이제 객체지향 프로그래밍의 기초를 마스터한 개발자입니다!** 🚀