---
chapter: Python 심화 개념 배우기 - "함수와 클래스의 첫걸음"
title: 함수 만들기 - 코드를 정리하는 마법
date: 2025-01-21
---

# 함수 만들기 - 코드를 정리하는 마법

## 🎯 이번 시간에 배울 것

- **함수가 무엇인지 이해하기**
- **반복되는 코드를 함수로 정리하는 방법**
- **AI와 함께 함수를 만들고 활용하기**

## 🌟 지금까지의 여정

### Chapter 1-5에서 배운 것들

**Chapter 1-4: Cursor 기본 기능** ✅
- Chat, Edit, Compose로 프로그램 만들기

**Chapter 5: Cursor 고급 기능** ✅  
- @파일명, 패턴 인식, 프로젝트 관리

**이제 Chapter 6에서는...** 🚀
- **Python 자체를 더 깊이 이해해서 더 멋진 프로그램을 만들어보겠습니다!**

## 🤔 함수가 왜 필요할까요?

### 😰 반복되는 코드의 문제점

**상황: 학생 점수 계산 프로그램**

```python
# 학생 1의 평균 계산
korean1 = 90
english1 = 85
math1 = 88
average1 = (korean1 + english1 + math1) / 3
print(f"학생1 평균: {average1:.1f}점")

# 학생 2의 평균 계산  
korean2 = 78
english2 = 92
math2 = 85
average2 = (korean2 + english2 + math2) / 3
print(f"학생2 평균: {average2:.1f}점")

# 학생 3의 평균 계산
korean3 = 95
english3 = 88
math3 = 92
average3 = (korean3 + english3 + math3) / 3
print(f"학생3 평균: {average3:.1f}점")
```

**문제점들:**
❌ 같은 계산 코드가 3번 반복됨  
❌ 계산 방식을 바꾸려면 3곳을 모두 수정해야 함  
❌ 학생이 더 많아지면 코드가 엄청 길어짐  
❌ 실수할 가능성이 높음

### ✨ 함수로 해결하기

```python
def calculate_average(korean, english, math):
    """3과목 평균을 계산하는 함수"""
    average = (korean + english + math) / 3
    return average

# 이제 간단하게!
student1_avg = calculate_average(90, 85, 88)
student2_avg = calculate_average(78, 92, 85)  
student3_avg = calculate_average(95, 88, 92)

print(f"학생1 평균: {student1_avg:.1f}점")
print(f"학생2 평균: {student2_avg:.1f}점")
print(f"학생3 평균: {student3_avg:.1f}점")
```

**장점들:**
✅ 코드가 훨씬 깔끔함  
✅ 계산 로직을 한 곳에서만 관리  
✅ 재사용 가능함  
✅ 실수 줄어듦

## 🚀 실습 1: 첫 번째 함수 만들기

### AI와 함께 함수 만들어보기

**Chat에서:**

```
함수를 처음 배우는 초심자입니다.

다음 기능을 함수로 만들어주세요:
- 두 숫자를 받아서 더하기, 빼기, 곱하기, 나누기를 계산
- 계산 결과를 예쁘게 출력

간단하고 이해하기 쉽게 만들어주시고,
함수가 어떻게 동작하는지도 설명해주세요.
```

### AI가 만들어준 함수

```python
def add_numbers(a, b):
    """두 숫자를 더하는 함수"""
    result = a + b
    print(f"{a} + {b} = {result}")
    return result

def subtract_numbers(a, b):
    """두 숫자를 빼는 함수"""
    result = a - b
    print(f"{a} - {b} = {result}")
    return result

def multiply_numbers(a, b):
    """두 숫자를 곱하는 함수"""
    result = a * b
    print(f"{a} × {b} = {result}")
    return result

def divide_numbers(a, b):
    """두 숫자를 나누는 함수"""
    if b != 0:
        result = a / b
        print(f"{a} ÷ {b} = {result}")
        return result
    else:
        print("0으로 나눌 수 없습니다!")
        return None

# 함수 사용해보기
add_numbers(10, 5)      # 10 + 5 = 15
subtract_numbers(10, 5) # 10 - 5 = 5
multiply_numbers(10, 5) # 10 × 5 = 50
divide_numbers(10, 5)   # 10 ÷ 5 = 2.0
```

### 🧠 함수 구조 이해하기

```python
def 함수이름(매개변수1, 매개변수2):
    """함수 설명 (독스트링)"""
    # 함수가 할 일
    결과 = 계산이나 처리
    return 결과  # 결과 반환 (선택사항)
```

**구성 요소:**
- **def**: 함수를 만든다는 키워드
- **함수이름**: 함수를 부를 때 사용할 이름
- **매개변수**: 함수에 전달할 값들
- **독스트링**: 함수가 무엇을 하는지 설명
- **return**: 함수의 결과를 돌려줌

## 🎯 실습 2: 실용적인 함수 만들기

### 메뉴 출력 함수

**기존 코드:**
```python
print("=" * 30)
print("🍕 피자 주문 프로그램")
print("=" * 30)
print("1. 페퍼로니 피자 - 15,000원")
print("2. 치즈 피자 - 12,000원")
print("3. 콤비네이션 피자 - 18,000원")
print("4. 종료")
print("=" * 30)
```

**함수로 만들기:**

**Chat에서:**
```
위의 메뉴 출력 코드를 함수로 만들어주세요.
그리고 다른 메뉴(음료, 치킨 등)도 비슷하게 출력할 수 있도록
유연하게 만들어주세요.
```

**AI가 만들어준 함수:**
```python
def show_menu(title, items, width=30):
    """메뉴를 예쁘게 출력하는 함수"""
    print("=" * width)
    print(f"🍕 {title}")
    print("=" * width)
    
    for i, item in enumerate(items, 1):
        print(f"{i}. {item}")
    
    print("=" * width)

# 사용 예시
pizza_menu = [
    "페퍼로니 피자 - 15,000원",
    "치즈 피자 - 12,000원", 
    "콤비네이션 피자 - 18,000원",
    "종료"
]

drink_menu = [
    "콜라 - 2,000원",
    "사이다 - 2,000원",
    "오렌지 주스 - 3,000원",
    "뒤로가기"
]

# 함수 사용
show_menu("피자 주문 프로그램", pizza_menu)
show_menu("음료 주문", drink_menu, 25)
```

### 입력 검증 함수

**기존 코드:**
```python
while True:
    age_input = input("나이를 입력하세요: ")
    if age_input.isdigit():
        age = int(age_input)
        if 1 <= age <= 100:
            break
        else:
            print("1~100 사이의 숫자를 입력해주세요!")
    else:
        print("숫자만 입력해주세요!")
```

**함수로 만들기:**
```python
def get_valid_number(prompt, min_val=1, max_val=100):
    """유효한 숫자 입력받는 함수"""
    while True:
        user_input = input(prompt)
        if user_input.isdigit():
            number = int(user_input)
            if min_val <= number <= max_val:
                return number
            else:
                print(f"{min_val}~{max_val} 사이의 숫자를 입력해주세요!")
        else:
            print("숫자만 입력해주세요!")

# 사용 예시
age = get_valid_number("나이를 입력하세요: ", 1, 100)
score = get_valid_number("점수를 입력하세요: ", 0, 100)
choice = get_valid_number("메뉴를 선택하세요: ", 1, 4)
```

## 🎮 실습 3: 게임에서 함수 활용하기

### 가위바위보 게임을 함수로 정리하기

**AI에게 요청:**
```
가위바위보 게임을 함수들로 나누어서 만들어주세요.

기능별로 함수 분리:
1. 컴퓨터 선택 함수
2. 결과 판정 함수  
3. 게임 한 라운드 함수
4. 게임 메인 함수

초심자가 이해하기 쉽게 만들어주세요.
```

**AI가 만들어준 게임:**
```python
import random

def get_computer_choice():
    """컴퓨터가 가위바위보 중 하나를 선택하는 함수"""
    choices = ["가위", "바위", "보"]
    return random.choice(choices)

def determine_winner(user_choice, computer_choice):
    """승부 결과를 판정하는 함수"""
    if user_choice == computer_choice:
        return "무승부"
    elif (user_choice == "가위" and computer_choice == "보") or \
         (user_choice == "바위" and computer_choice == "가위") or \
         (user_choice == "보" and computer_choice == "바위"):
        return "사용자 승리"
    else:
        return "컴퓨터 승리"

def play_one_round():
    """가위바위보 한 라운드를 진행하는 함수"""
    print("\n🎮 가위바위보 게임!")
    user_choice = input("가위, 바위, 보 중 하나를 입력하세요: ")
    
    if user_choice not in ["가위", "바위", "보"]:
        print("❌ 가위, 바위, 보 중에서 선택해주세요!")
        return None
    
    computer_choice = get_computer_choice()
    result = determine_winner(user_choice, computer_choice)
    
    print(f"당신: {user_choice}")
    print(f"컴퓨터: {computer_choice}")
    print(f"결과: {result}")
    
    return result

def play_game():
    """가위바위보 게임 메인 함수"""
    wins = 0
    losses = 0
    draws = 0
    
    print("🌟 가위바위보 게임을 시작합니다!")
    
    while True:
        result = play_one_round()
        
        if result == "사용자 승리":
            wins += 1
            print("🎉 이겼습니다!")
        elif result == "컴퓨터 승리":
            losses += 1
            print("😢 졌습니다!")
        elif result == "무승부":
            draws += 1
            print("🤝 비겼습니다!")
        
        continue_game = input("\n계속 하시겠습니까? (y/n): ")
        if continue_game.lower() != 'y':
            break
    
    print(f"\n📊 최종 결과:")
    print(f"승리: {wins}회, 패배: {losses}회, 무승부: {draws}회")
    print("게임 종료!")

# 게임 시작
play_game()
```

## 🌟 학습 성과 체크

### 오늘 배운 내용

```
✅ 함수가 무엇인지 이해하기
✅ 반복되는 코드를 함수로 정리하기
✅ 매개변수와 반환값 사용하기
✅ 실용적인 함수들 만들어보기
```

### 함수의 장점

**Before (함수 없이):**
- 코드가 길고 복잡함
- 같은 코드가 여러 번 반복됨
- 수정할 때 여러 곳을 바꿔야 함

**After (함수 활용):**
- 코드가 깔끔하고 정리됨
- 재사용 가능한 코드 블록
- 수정이 쉽고 실수가 줄어듦

## 🎯 오늘의 과제

**함수 마스터하기:**

1. **계산기 함수 만들기:**
   - 사칙연산 함수들 (add, subtract, multiply, divide)
   - 공학 계산 함수들 (제곱, 제곱근, 원의 넓이 등)

2. **유틸리티 함수 만들기:**
   - 문자열 관련 함수 (대문자 변환, 단어 개수 세기 등)
   - 리스트 관련 함수 (최댓값, 최솟값, 평균 등)

3. **게임 함수 만들기:**
   - 숫자 맞추기 게임을 함수로 분리
   - 단어 맞추기 게임 함수들

**AI 활용 팁:**
- "이 코드를 함수로 만들어주세요"
- "함수를 더 간단하게 만들어주세요"
- "이 함수에 에러 처리를 추가해주세요"

## 💡 다음 시간 예고

다음 Chapter 6-2에서는 **클래스 기초 - 객체 만들기**를 배워보겠습니다!

**여러분은 이제 코드를 체계적으로 정리할 수 있는 함수 마스터입니다!** 🚀