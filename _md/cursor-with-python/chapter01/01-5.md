---
chapter: Cursor와 함께하는 첫 코딩 여행
title: 실습 - AI와 함께 첫 번째 프로그램 만들기
date: 2025-07-09
---

# 실습: AI와 함께 첫 번째 프로그램 만들기

## 🎯 이번 섹션에서 배울 것

- AI와 협업하여 완전한 프로그램 만들기
- 문제 해결 과정 체험하기
- 지금까지 배운 모든 기능 종합 활용
- 실제 사용 가능한 프로그램 완성하기

## 🤔 어떤 프로그램을 만들까요?

**오늘의 목표: 실용적인 계산기 프로그램 만들기**

### 💡 왜 계산기를 만들까요?

```
🧮 계산기 프로그램의 장점:
- 초보자도 이해하기 쉬움
- 실제로 사용할 수 있음
- 다양한 프로그래밍 개념 포함
- 단계별로 기능 추가 가능

🎮 게임 같은 재미:
- 기본 계산 → 고급 계산 → 메모리 기능
- 점점 발전하는 재미를 느낄 수 있어요!
```

### 🎯 만들어볼 프로그램의 기능

**1단계: 기본 계산기**

```
- 덧셈, 뺄셈, 곱셈, 나눗셈
- 사용자 입력 받기
- 결과 출력하기
```

**2단계: 고급 계산기**

```
- 제곱, 제곱근 계산
- 메뉴 시스템 추가
- 계속 계산하기 기능
```

**3단계: 완전한 계산기**

```
- 계산 기록 저장
- 에러 처리 추가
- 이전 결과 불러오기
```

## 🚀 1단계: 기본 계산기 만들기

### AI와 대화하며 설계하기

**Chat에서 시작:**

```
Chat에 입력:
"간단한 계산기 프로그램을 만들고 싶어요. 
두 숫자를 입력받고 사칙연산을 할 수 있는 프로그램을 만들어주세요."
```

**예상 AI 답변:**

```python
# 기본 계산기 프로그램
def calculator():
    print("=== 간단한 계산기 ===")
    
    # 숫자 입력 받기
    num1 = float(input("첫 번째 숫자를 입력하세요: "))
    num2 = float(input("두 번째 숫자를 입력하세요: "))
    
    # 연산 선택
    operation = input("연산을 선택하세요 (+, -, *, /): ")
    
    # 계산 수행
    if operation == '+':
        result = num1 + num2
    elif operation == '-':
        result = num1 - num2
    elif operation == '*':
        result = num1 * num2
    elif operation == '/':
        if num2 != 0:
            result = num1 / num2
        else:
            print("0으로 나눌 수 없습니다!")
            return
    else:
        print("잘못된 연산입니다!")
        return
    
    print(f"결과: {num1} {operation} {num2} = {result}")

# 계산기 실행
calculator()
```

### 🎮 코드 이해하기

**AI에게 설명 요청:**

```
Chat에 입력:
"위 코드를 초보자도 이해할 수 있게 한 줄씩 설명해주세요"
```

**주요 개념 설명:**

```python
# 함수 정의: 계산기 기능을 묶어놓은 상자
def calculator():
    
    # 사용자 입력: input() 함수로 키보드 입력 받기
    num1 = float(input("첫 번째 숫자를 입력하세요: "))
    
    # 조건문: 사용자가 선택한 연산에 따라 다른 계산 수행
    if operation == '+':
        result = num1 + num2
    
    # 에러 처리: 0으로 나누는 경우 방지
    if num2 != 0:
        result = num1 / num2
    else:
        print("0으로 나눌 수 없습니다!")
```

## 🔧 2단계: 코드 개선하기

### Edit 기능으로 코드 개선

**1. 코드 선택 후 Ctrl+K:**

```
선택할 부분:
def calculator():
    print("=== 간단한 계산기 ===")
    
    # 숫자 입력 받기
    num1 = float(input("첫 번째 숫자를 입력하세요: "))
    num2 = float(input("두 번째 숫자를 입력하세요: "))

요청 내용:
"이 부분에 입력 검증 기능을 추가해주세요"
```

**2. 개선된 코드:**

```python
def calculator():
    print("=== 간단한 계산기 ===")
    
    # 숫자 입력 받기 (에러 처리 추가)
    try:
        num1 = float(input("첫 번째 숫자를 입력하세요: "))
        num2 = float(input("두 번째 숫자를 입력하세요: "))
    except ValueError:
        print("올바른 숫자를 입력해주세요!")
        return
    
    # 연산 선택
    operation = input("연산을 선택하세요 (+, -, *, /): ")
    
    # 계산 수행
    if operation == '+':
        result = num1 + num2
    elif operation == '-':
        result = num1 - num2
    elif operation == '*':
        result = num1 * num2
    elif operation == '/':
        if num2 != 0:
            result = num1 / num2
        else:
            print("0으로 나눌 수 없습니다!")
            return
    else:
        print("잘못된 연산입니다!")
        return
    
    print(f"결과: {num1} {operation} {num2} = {result}")
```

### 🎯 새로운 기능 추가

**Compose 기능 활용 (Ctrl+I):**

```
빈 줄에서 Ctrl+I 후 요청:
"계산기에 계속 계산할지 묻는 기능을 추가해주세요"
```

**추가된 기능:**

```python
def calculator():
    while True:
        print("\n=== 간단한 계산기 ===")
        
        # 숫자 입력 받기
        try:
            num1 = float(input("첫 번째 숫자를 입력하세요: "))
            num2 = float(input("두 번째 숫자를 입력하세요: "))
        except ValueError:
            print("올바른 숫자를 입력해주세요!")
            continue
        
        # 연산 선택
        operation = input("연산을 선택하세요 (+, -, *, /): ")
        
        # 계산 수행
        if operation == '+':
            result = num1 + num2
        elif operation == '-':
            result = num1 - num2
        elif operation == '*':
            result = num1 * num2
        elif operation == '/':
            if num2 != 0:
                result = num1 / num2
            else:
                print("0으로 나눌 수 없습니다!")
                continue
        else:
            print("잘못된 연산입니다!")
            continue
        
        print(f"결과: {num1} {operation} {num2} = {result}")
        
        # 계속 계산할지 묻기
        continue_calc = input("계속 계산하시겠습니까? (y/n): ")
        if continue_calc.lower() != 'y':
            print("계산기를 종료합니다.")
            break

# 계산기 실행
calculator()
```

## 🌟 3단계: 고급 기능 추가

### 메뉴 시스템 추가

**AI에게 요청:**

```
Chat에 입력:
"계산기에 메뉴 시스템을 추가해주세요. 
기본 계산과 고급 계산(제곱, 제곱근)을 선택할 수 있게 해주세요."
```

**완성된 고급 계산기:**

```python
import math

def show_menu():
    print("\n=== 고급 계산기 ===")
    print("1. 기본 계산 (+, -, *, /)")
    print("2. 제곱 계산")
    print("3. 제곱근 계산")
    print("4. 종료")

def basic_calculator():
    try:
        num1 = float(input("첫 번째 숫자를 입력하세요: "))
        num2 = float(input("두 번째 숫자를 입력하세요: "))
    except ValueError:
        print("올바른 숫자를 입력해주세요!")
        return
    
    operation = input("연산을 선택하세요 (+, -, *, /): ")
    
    if operation == '+':
        result = num1 + num2
    elif operation == '-':
        result = num1 - num2
    elif operation == '*':
        result = num1 * num2
    elif operation == '/':
        if num2 != 0:
            result = num1 / num2
        else:
            print("0으로 나눌 수 없습니다!")
            return
    else:
        print("잘못된 연산입니다!")
        return
    
    print(f"결과: {num1} {operation} {num2} = {result}")

def power_calculator():
    try:
        base = float(input("밑수를 입력하세요: "))
        exponent = float(input("지수를 입력하세요: "))
    except ValueError:
        print("올바른 숫자를 입력해주세요!")
        return
    
    result = base ** exponent
    print(f"결과: {base}^{exponent} = {result}")

def sqrt_calculator():
    try:
        num = float(input("제곱근을 구할 숫자를 입력하세요: "))
    except ValueError:
        print("올바른 숫자를 입력해주세요!")
        return
    
    if num < 0:
        print("음수의 제곱근은 계산할 수 없습니다!")
        return
    
    result = math.sqrt(num)
    print(f"결과: √{num} = {result}")

def main():
    while True:
        show_menu()
        choice = input("선택하세요: ")
        
        if choice == '1':
            basic_calculator()
        elif choice == '2':
            power_calculator()
        elif choice == '3':
            sqrt_calculator()
        elif choice == '4':
            print("계산기를 종료합니다.")
            break
        else:
            print("잘못된 선택입니다!")

# 프로그램 실행
if __name__ == "__main__":
    main()
```

## 🎮 4단계: 최종 개선

### 계산 기록 기능 추가

**AI에게 요청:**

```
Chat에 입력:
"계산 기록을 저장하고 볼 수 있는 기능을 추가해주세요"
```

**최종 완성된 계산기:**

```python
import math
from datetime import datetime

# 계산 기록 저장용 리스트
calculation_history = []

def show_menu():
    print("\n=== 완전한 계산기 ===")
    print("1. 기본 계산 (+, -, *, /)")
    print("2. 제곱 계산")
    print("3. 제곱근 계산")
    print("4. 계산 기록 보기")
    print("5. 계산 기록 지우기")
    print("6. 종료")

def add_to_history(calculation, result):
    timestamp = datetime.now().strftime("%Y-%m-%d %H:%M:%S")
    calculation_history.append({
        'time': timestamp,
        'calculation': calculation,
        'result': result
    })

def basic_calculator():
    try:
        num1 = float(input("첫 번째 숫자를 입력하세요: "))
        num2 = float(input("두 번째 숫자를 입력하세요: "))
    except ValueError:
        print("올바른 숫자를 입력해주세요!")
        return
    
    operation = input("연산을 선택하세요 (+, -, *, /): ")
    
    if operation == '+':
        result = num1 + num2
    elif operation == '-':
        result = num1 - num2
    elif operation == '*':
        result = num1 * num2
    elif operation == '/':
        if num2 != 0:
            result = num1 / num2
        else:
            print("0으로 나눌 수 없습니다!")
            return
    else:
        print("잘못된 연산입니다!")
        return
    
    calculation = f"{num1} {operation} {num2}"
    print(f"결과: {calculation} = {result}")
    add_to_history(calculation, result)

def power_calculator():
    try:
        base = float(input("밑수를 입력하세요: "))
        exponent = float(input("지수를 입력하세요: "))
    except ValueError:
        print("올바른 숫자를 입력해주세요!")
        return
    
    result = base ** exponent
    calculation = f"{base}^{exponent}"
    print(f"결과: {calculation} = {result}")
    add_to_history(calculation, result)

def sqrt_calculator():
    try:
        num = float(input("제곱근을 구할 숫자를 입력하세요: "))
    except ValueError:
        print("올바른 숫자를 입력해주세요!")
        return
    
    if num < 0:
        print("음수의 제곱근은 계산할 수 없습니다!")
        return
    
    result = math.sqrt(num)
    calculation = f"√{num}"
    print(f"결과: {calculation} = {result}")
    add_to_history(calculation, result)

def show_history():
    if not calculation_history:
        print("계산 기록이 없습니다.")
        return
    
    print("\n=== 계산 기록 ===")
    for i, record in enumerate(calculation_history, 1):
        print(f"{i}. [{record['time']}] {record['calculation']} = {record['result']}")

def clear_history():
    global calculation_history
    calculation_history = []
    print("계산 기록이 지워졌습니다.")

def main():
    print("완전한 계산기 프로그램에 오신 것을 환영합니다!")
    
    while True:
        show_menu()
        choice = input("선택하세요: ")
        
        if choice == '1':
            basic_calculator()
        elif choice == '2':
            power_calculator()
        elif choice == '3':
            sqrt_calculator()
        elif choice == '4':
            show_history()
        elif choice == '5':
            clear_history()
        elif choice == '6':
            print("계산기를 종료합니다. 감사합니다!")
            break
        else:
            print("잘못된 선택입니다!")

# 프로그램 실행
if __name__ == "__main__":
    main()
```

## 🎯 실습 과제: 직접 만들어보기

### 과제 1: 단위 변환 계산기

**AI에게 요청해보세요:**

```
Chat에 입력:
"온도 변환 계산기를 만들어주세요. 
섭씨를 화씨로, 화씨를 섭씨로 변환하는 기능이 있어야 합니다."
```

**예상 결과:**

```python
def temperature_converter():
    print("\n=== 온도 변환 계산기 ===")
    print("1. 섭씨 → 화씨")
    print("2. 화씨 → 섭씨")
    
    choice = input("선택하세요: ")
    
    if choice == '1':
        celsius = float(input("섭씨 온도를 입력하세요: "))
        fahrenheit = (celsius * 9/5) + 32
        print(f"{celsius}°C = {fahrenheit}°F")
    elif choice == '2':
        fahrenheit = float(input("화씨 온도를 입력하세요: "))
        celsius = (fahrenheit - 32) * 5/9
        print(f"{fahrenheit}°F = {celsius}°C")
    else:
        print("잘못된 선택입니다!")

temperature_converter()
```

### 과제 2: 할일 관리 프로그램

**AI에게 요청해보세요:**

```
Chat에 입력:
"할일을 추가하고, 완료하고, 목록을 볼 수 있는 간단한 프로그램을 만들어주세요"
```

## 🔍 코드 분석하기

### 🤔 AI가 만든 코드를 이해해보자

**다음 질문들을 AI에게 해보세요:**

1. **함수의 역할:**

```
"main() 함수가 하는 일을 설명해주세요"
```

2. **에러 처리:**

```
"try-except 구문이 왜 필요한지 설명해주세요"
```

3. **데이터 저장:**

```
"calculation_history 리스트는 어떻게 작동하나요?"
```

4. **시간 처리:**

```
"datetime을 사용하는 이유가 뭐죠?"
```

## 🌟 추가 개선 아이디어

### AI에게 이런 기능도 요청해보세요

**1. 통계 기능:**

```
"계산 기록에서 가장 자주 사용한 연산을 찾는 기능을 추가해주세요"
```

**2. 파일 저장:**

```
"계산 기록을 텍스트 파일로 저장하는 기능을 추가해주세요"
```

**3. 그래프 기능:**

```
"계산 결과를 간단한 그래프로 보여주는 기능을 추가해주세요"
```

## 🚨 문제 해결 연습

### 자주 발생하는 오류들

**1. 숫자 입력 오류:**

```python
# 오류 상황
num = float(input("숫자를 입력하세요: "))
# 사용자가 "abc" 입력 시 → ValueError

# 해결 방법
try:
    num = float(input("숫자를 입력하세요: "))
except ValueError:
    print("올바른 숫자를 입력해주세요!")
```

**2. 0으로 나누기 오류:**

```python
# 오류 상황
result = num1 / num2  # num2가 0이면 ZeroDivisionError

# 해결 방법
if num2 != 0:
    result = num1 / num2
else:
    print("0으로 나눌 수 없습니다!")
```

**3. 메뉴 선택 오류:**

```python
# 오류 상황
choice = input("선택하세요: ")
# 사용자가 1,2,3,4,5,6 외의 값 입력 시

# 해결 방법
if choice in ['1', '2', '3', '4', '5', '6']:
    # 올바른 선택
else:
    print("잘못된 선택입니다!")
```

## 💡 코딩 실력 향상 팁

### 1. 코드 리뷰 습관

**AI에게 코드 검토 요청:**

```
Chat에 입력:
"이 코드에서 개선할 점이 있나요?"
```

### 2. 다른 방법 탐색

**AI에게 다른 구현 방법 문의:**

```
Chat에 입력:
"같은 기능을 더 간단하게 구현할 수 있나요?"
```

### 3. 확장 기능 아이디어

**AI에게 기능 확장 제안 요청:**

```
Chat에 입력:
"이 계산기에 추가할 만한 재미있는 기능이 있나요?"
```

## 🎉 완성도 체크리스트

### 기본 기능 완성

```
✅ 완료 체크:
- [ ] 기본 사칙연산 (+, -, *, /)
- [ ] 사용자 입력 받기
- [ ] 결과 출력하기
- [ ] 에러 처리 (0으로 나누기, 잘못된 입력)
```

### 고급 기능 완성

```
✅ 완료 체크:
- [ ] 메뉴 시스템
- [ ] 제곱, 제곱근 계산
- [ ] 계속 계산하기 기능
- [ ] 계산 기록 저장/조회
```

### 코드 품질 체크

```
✅ 완료 체크:
- [ ] 함수별로 기능 분리
- [ ] 에러 처리 완비
- [ ] 사용자 친화적 메시지
- [ ] 코드 주석 추가
```

## 💬 다음 단계 예고

축하합니다! 여러분은 이제 AI와 협업하는 새로운 방식의 프로그래밍**을 경험했습니다! 🎊

다음 Chapter에서는 더 고급 기능들을 배워보겠습니다:

- **Edit 기능**으로 코드를 더 효율적으로 수정하기
- **Compose 기능**으로 더 복잡한 프로그램 만들기
- **실제 프로젝트**에서 AI 활용하기

## 🎯 추가 연습 과제

### 도전 과제 1: 게임 만들기

```
AI에게 요청:
"숫자 맞추기 게임을 만들어주세요. 
컴퓨터가 1-100 사이 숫자를 생각하고 사용자가 맞추는 게임입니다."
```

### 도전 과제 2: 데이터 관리

```
AI에게 요청:
"학생 성적을 관리하는 프로그램을 만들어주세요. 
학생 추가, 성적 입력, 평균 계산 기능이 있어야 합니다."
```

### 도전 과제 3: 파일 처리

```
AI에게 요청:
"텍스트 파일을 읽어서 단어 수를 세는 프로그램을 만들어주세요"
```

## 🌟 마무리

여러분은 이제 **AI와 협업하는 새로운 방식의 프로그래밍**을 경험했습니다!

### 🔥 오늘 달성한 것들

- ✅ 완전한 계산기 프로그램 완성
- ✅ AI와 협업하는 개발 프로세스 체험
- ✅ 문제 해결 능력 향상
- ✅ 코드 개선 및 기능 확장 경험

### 💡 중요한 깨달음

- AI는 도구일 뿐, 여러분의 아이디어와 판단이 중요
- 단계별로 접근하면 복잡한 프로그램도 만들 수 있음
- 에러는 두려워할 것이 아니라 학습의 기회
- 지속적인 개선이 좋은 프로그램을 만드는 비결

**이제 여러분은 진정한 AI 시대의 프로그래머가 되었습니다! 🚀**
