---
chapter: Chat으로 Python 마스터하기 - "궁금한 건 다 물어봐"
title: 실습 - "간단한 게임" 만들어보기 (가위바위보, 숫자맞추기)
date: 2025-07-14
---

# 02-4. 실습: "간단한 게임" 만들어보기 (가위바위보, 숫자맞추기)

> **"이제 진짜 재미있는 것을 만들어볼까요? 게임 개발자가 된 기분이에요!"**

안녕하세요!

**🌟 Chapter 2.3을 완주하신 분들:**
지금까지 정말 많은 것들을 배우셨네요! 🎉

**🎯 Chapter 2.4부터 시작하시는 분들:**
걱정하지 마세요! 이번 시간에는 재미있는 게임을 만들면서 프로그래밍의 즐거움을 느껴보겠습니다.

## 📚 이전 내용 간단 요약 (선택 사항)

**Chapter 2.1~2.3에서 배운 핵심 개념:**

```python
# 기본 구조 (Chapter 2.1)
name = "김코딩"
def calculate_grade(score):
    if score >= 90:
        return 'A'
    # ...

# 여러 데이터 처리 (Chapter 2.2)
students = ["김코딩", "이파이썬", "박데이터"]
for student in students:
    print(student)

# 에러 처리 (Chapter 2.3)
try:
    age = int(input("나이: "))
except ValueError:
    print("숫자를 입력해주세요!")
```

**💡 핵심 아이디어:**

- 기본 문법으로 프로그램 작성
- 여러 데이터 처리
- 안전한 에러 처리
- 이제 이 모든 것을 조합해서 게임을 만들어보자!

## 💡 지금까지의 학습 여정

**Chapter 2에서 마스터한 핵심 기술들:**

```
✅ 변수, 함수, 조건문의 기초 (2.1)
✅ 리스트와 for문으로 데이터 처리 (2.2)
✅ 에러 처리와 AI 협업 디버깅 (2.3)
✅ 완전한 프로그램 설계 및 구현 능력
```

**이제 이 모든 지식을 결합해서 무엇을 만들 수 있을까요?**

바로 **진짜 재미있는 게임**을 만들어볼 시간입니다! 🎮

### 🎯 왜 게임을 만들까요?

```
🎮 게임 개발의 학습 효과:
- 모든 프로그래밍 개념 종합 활용
- 사용자 상호작용 경험
- 로직 설계 및 문제 해결 능력 향상
- 성취감과 재미를 동시에!
```

## 🎯 이번 시간에 만들 게임들

- **가위바위보 게임**: 컴퓨터와 대결하는 클래식 게임
- **숫자맞추기 게임**: 추리력을 시험하는 두뇌 게임
- **게임 통합 시스템**: 두 게임을 하나로 합친 미니 게임센터

## 📋 시작하기 전에

**Cursor를 열고 새 Python 파일을 만들어주세요:**

1. `Ctrl+N`으로 새 파일 생성
2. `Ctrl+S`로 저장 → `mini_games.py`로 저장
3. Chat 패널 열기 (`Ctrl+L`)

## 🎮 PART 1: 가위바위보 게임 만들기

### 게임 설계부터 시작하기

**Chat에 입력:**

```
가위바위보 게임을 만들고 싶어요.
어떤 기능들이 필요하고 어떻게 설계하면 좋을까요?
```

### 🤖 AI와 함께 게임 설계

AI가 제안할 설계:

```
가위바위보 게임 필요 기능:

1. 사용자 입력 받기 (가위/바위/보)
2. 컴퓨터 선택 랜덤 생성
3. 승부 판정 로직
4. 점수 시스템
5. 게임 반복 및 종료
6. 에러 처리 (잘못된 입력)
```

### 🎯 실습 1: 기본 가위바위보 만들기

**Chat에 요청:**

```
간단한 가위바위보 게임을 단계별로 만들어주세요.
먼저 기본 버전부터 시작해주세요.
```

AI가 만들어줄 기본 버전:

```python
import random

def rock_paper_scissors_basic():
    """기본 가위바위보 게임"""
    choices = ['가위', '바위', '보']
    
    print("=== 가위바위보 게임 ===")
    print("가위, 바위, 보 중 하나를 입력하세요!")
    
    # 사용자 입력
    user_choice = input("당신의 선택: ")
    
    # 컴퓨터 선택
    computer_choice = random.choice(choices)
    
    print(f"당신: {user_choice}")
    print(f"컴퓨터: {computer_choice}")
    
    # 승부 판정
    if user_choice == computer_choice:
        print("비겼습니다!")
    elif (user_choice == "가위" and computer_choice == "보") or \
         (user_choice == "바위" and computer_choice == "가위") or \
         (user_choice == "보" and computer_choice == "바위"):
        print("당신이 이겼습니다! 🎉")
    else:
        print("컴퓨터가 이겼습니다! 😅")

# 게임 실행
rock_paper_scissors_basic()
```

**실행 후 Chat에서 질문:**

```
이 코드를 더 좋게 만들 수 있는 방법이 있나요?
에러 처리와 반복 기능을 추가해주세요.
```

### 🎯 실습 2: 고급 가위바위보 게임

**Chat에 요청:**

```
에러 처리, 점수 시스템, 반복 기능이 있는 
완전한 가위바위보 게임을 만들어주세요.
```

AI가 만들어줄 고급 버전:

```python
import random

class RockPaperScissors:
    def __init__(self):
        self.choices = ['가위', '바위', '보']
        self.user_score = 0
        self.computer_score = 0
        self.total_games = 0
    
    def get_user_choice(self):
        """사용자 선택 입력받기 (에러 처리 포함)"""
        while True:
            try:
                choice = input("\n가위, 바위, 보 중 선택하세요 (종료: q): ").strip()
                
                if choice.lower() == 'q':
                    return None
                
                if choice in self.choices:
                    return choice
                else:
                    print("❌ 가위, 바위, 보 중에서만 선택해주세요!")
                    
            except KeyboardInterrupt:
                print("\n게임을 종료합니다.")
                return None
    
    def determine_winner(self, user_choice, computer_choice):
        """승부 판정"""
        if user_choice == computer_choice:
            return "tie"
        
        winning_conditions = {
            "가위": "보",
            "바위": "가위", 
            "보": "바위"
        }
        
        if winning_conditions[user_choice] == computer_choice:
            return "user"
        else:
            return "computer"
    
    def display_result(self, user_choice, computer_choice, result):
        """결과 출력"""
        print(f"\n{'='*30}")
        print(f"🎮 당신: {user_choice}")
        print(f"🤖 컴퓨터: {computer_choice}")
        print(f"{'='*30}")
        
        if result == "tie":
            print("🤝 비겼습니다!")
        elif result == "user":
            print("🎉 당신이 이겼습니다!")
            self.user_score += 1
        else:
            print("😅 컴퓨터가 이겼습니다!")
            self.computer_score += 1
        
        self.total_games += 1
    
    def show_score(self):
        """현재 점수 표시"""
        print(f"\n📊 현재 점수:")
        print(f"당신: {self.user_score}승")
        print(f"컴퓨터: {self.computer_score}승")
        print(f"비김: {self.total_games - self.user_score - self.computer_score}회")
        print(f"총 게임: {self.total_games}회")
        
        if self.total_games > 0:
            win_rate = (self.user_score / self.total_games) * 100
            print(f"승률: {win_rate:.1f}%")
    
    def play(self):
        """게임 실행"""
        print("🎮 가위바위보 게임에 오신 것을 환영합니다!")
        print("언제든지 'q'를 입력하면 게임을 종료할 수 있습니다.")
        
        while True:
            user_choice = self.get_user_choice()
            
            if user_choice is None:  # 게임 종료
                break
            
            computer_choice = random.choice(self.choices)
            result = self.determine_winner(user_choice, computer_choice)
            
            self.display_result(user_choice, computer_choice, result)
            self.show_score()
        
        print(f"\n🎯 최종 결과:")
        self.show_score()
        print("게임을 종료합니다. 다음에 또 만나요! 👋")

# 게임 실행
if __name__ == "__main__":
    game = RockPaperScissors()
    game.play()
```

## 🔢 PART 2: 숫자맞추기 게임 만들기

### 게임 기획하기

**Chat에 질문:**

```
숫자맞추기 게임을 만들고 싶어요.
어떤 기능들이 있으면 재미있을까요?
```

### 🎯 실습 3: 기본 숫자맞추기 게임

**Chat에 요청:**

```
1부터 100까지 숫자 중 하나를 맞추는 게임을 만들어주세요.
힌트 기능과 시도 횟수 제한도 포함해주세요.
```

AI가 만들어줄 숫자맞추기 게임:

```python
import random

class NumberGuessingGame:
    def __init__(self, min_num=1, max_num=100, max_attempts=7):
        self.min_num = min_num
        self.max_num = max_num
        self.max_attempts = max_attempts
        self.secret_number = None
        self.attempts = 0
        self.guesses = []
    
    def start_new_game(self):
        """새 게임 시작"""
        self.secret_number = random.randint(self.min_num, self.max_num)
        self.attempts = 0
        self.guesses = []
        
        print(f"\n🎯 숫자맞추기 게임!")
        print(f"📋 {self.min_num}부터 {self.max_num}까지의 숫자를 생각했습니다.")
        print(f"🎮 {self.max_attempts}번의 기회가 있습니다!")
        print("💡 'hint'를 입력하면 힌트를 받을 수 있어요!")
    
    def get_user_guess(self):
        """사용자 추측 입력받기"""
        while True:
            try:
                user_input = input(f"\n추측 ({self.attempts + 1}/{self.max_attempts}): ").strip().lower()
                
                if user_input == 'q':
                    return None
                
                if user_input == 'hint':
                    return 'hint'
                
                guess = int(user_input)
                
                if self.min_num <= guess <= self.max_num:
                    return guess
                else:
                    print(f"❌ {self.min_num}부터 {self.max_num} 사이의 숫자를 입력해주세요!")
                    
            except ValueError:
                print("❌ 숫자를 입력해주세요! (종료: q, 힌트: hint)")
    
    def give_hint(self):
        """힌트 제공"""
        if not self.guesses:
            print("💡 힌트: 먼저 한 번 추측해보세요!")
            return
        
        last_guess = self.guesses[-1]
        difference = abs(self.secret_number - last_guess)
        
        if difference <= 5:
            print("🔥 매우 가까워요!")
        elif difference <= 15:
            print("😊 가까워요!")
        elif difference <= 30:
            print("🤔 보통이에요.")
        else:
            print("❄️ 멀어요!")
        
        # 홀짝 힌트
        if self.secret_number % 2 == 0:
            print("💡 추가 힌트: 짝수입니다!")
        else:
            print("💡 추가 힌트: 홀수입니다!")
    
    def check_guess(self, guess):
        """추측 확인"""
        self.attempts += 1
        self.guesses.append(guess)
        
        if guess == self.secret_number:
            return "correct"
        elif guess < self.secret_number:
            return "higher"
        else:
            return "lower"
    
    def display_guess_result(self, guess, result):
        """추측 결과 출력"""
        if result == "correct":
            print(f"\n🎉 정답입니다! {self.secret_number}이 맞아요!")
            print(f"👏 {self.attempts}번 만에 맞추셨네요!")
            
            if self.attempts <= 3:
                print("🏆 천재적이에요!")
            elif self.attempts <= 5:
                print("👍 훌륭해요!")
            else:
                print("😊 좋은 추리력이에요!")
                
        elif result == "higher":
            print(f"📈 {guess}보다 큰 숫자예요!")
        else:
            print(f"📉 {guess}보다 작은 숫자예요!")
        
        if len(self.guesses) > 1:
            print(f"🔍 지금까지 추측: {', '.join(map(str, self.guesses))}")
    
    def play(self):
        """게임 실행"""
        print("🎯 숫자맞추기 게임에 오신 것을 환영합니다!")
        
        while True:
            self.start_new_game()
            
            while self.attempts < self.max_attempts:
                guess = self.get_user_guess()
                
                if guess is None:  # 게임 종료
                    print("게임을 종료합니다.")
                    return
                
                if guess == 'hint':
                    self.give_hint()
                    continue
                
                result = self.check_guess(guess)
                self.display_guess_result(guess, result)
                
                if result == "correct":
                    break
            
            else:  # 모든 기회를 사용했을 때
                print(f"\n💥 게임 오버! 정답은 {self.secret_number}이었습니다.")
            
            # 다시 하기
            while True:
                play_again = input("\n🔄 다시 하시겠어요? (y/n): ").strip().lower()
                if play_again in ['y', 'yes', '네', 'ㅇ']:
                    break
                elif play_again in ['n', 'no', '아니오', 'ㄴ']:
                    print("게임을 종료합니다. 즐거우셨나요? 👋")
                    return
                else:
                    print("y 또는 n을 입력해주세요.")

# 게임 실행
if __name__ == "__main__":
    game = NumberGuessingGame()
    game.play()
```

## 🎮 PART 3: 미니 게임센터 만들기

### 두 게임을 하나로 합치기

**Chat에 요청:**

```
지금까지 만든 가위바위보와 숫자맞추기 게임을 
하나의 미니 게임센터로 합쳐주세요.
메뉴 시스템과 게임 기록 기능도 추가해주세요.
```

### 🎯 실습 4: 완전한 미니 게임센터

AI가 만들어줄 게임센터:

```python
import random
import json
import os
from datetime import datetime

class GameCenter:
    def __init__(self):
        self.games = {
            '1': '가위바위보',
            '2': '숫자맞추기'
        }
        self.stats_file = 'game_stats.json'
        self.load_stats()
    
    def load_stats(self):
        """게임 통계 로드"""
        try:
            if os.path.exists(self.stats_file):
                with open(self.stats_file, 'r', encoding='utf-8') as f:
                    self.stats = json.load(f)
            else:
                self.stats = {
                    'rock_paper_scissors': {'wins': 0, 'losses': 0, 'ties': 0},
                    'number_guessing': {'wins': 0, 'losses': 0, 'best_attempts': 999}
                }
        except:
            self.stats = {
                'rock_paper_scissors': {'wins': 0, 'losses': 0, 'ties': 0},
                'number_guessing': {'wins': 0, 'losses': 0, 'best_attempts': 999}
            }
    
    def save_stats(self):
        """게임 통계 저장"""
        try:
            with open(self.stats_file, 'w', encoding='utf-8') as f:
                json.dump(self.stats, f, ensure_ascii=False, indent=2)
        except:
            print("⚠️ 통계 저장에 실패했습니다.")
    
    def show_main_menu(self):
        """메인 메뉴 표시"""
        print("\n" + "="*50)
        print("🎮 미니 게임센터에 오신 것을 환영합니다!")
        print("="*50)
        print("1. 🪨📄✂️  가위바위보")
        print("2. 🔢 숫자맞추기")
        print("3. 📊 게임 기록 보기")
        print("4. 🏆 도전 과제")
        print("5. ❓ 도움말")
        print("6. 👋 종료")
        print("="*50)
    
    def rock_paper_scissors_game(self):
        """가위바위보 게임 (간소화된 버전)"""
        choices = ['가위', '바위', '보']
        wins = losses = ties = 0
        
        print("\n🪨📄✂️ 가위바위보 게임!")
        print("'q'를 입력하면 메인 메뉴로 돌아갑니다.")
        
        while True:
            try:
                user_choice = input("\n가위, 바위, 보 중 선택 (q: 종료): ").strip()
                
                if user_choice.lower() == 'q':
                    break
                
                if user_choice not in choices:
                    print("❌ 가위, 바위, 보 중에서만 선택해주세요!")
                    continue
                
                computer_choice = random.choice(choices)
                print(f"🎮 당신: {user_choice} | 🤖 컴퓨터: {computer_choice}")
                
                if user_choice == computer_choice:
                    print("🤝 비겼습니다!")
                    ties += 1
                elif ((user_choice == "가위" and computer_choice == "보") or
                      (user_choice == "바위" and computer_choice == "가위") or
                      (user_choice == "보" and computer_choice == "바위")):
                    print("🎉 이겼습니다!")
                    wins += 1
                else:
                    print("😅 졌습니다!")
                    losses += 1
                
                print(f"현재 기록: {wins}승 {losses}패 {ties}무")
                
            except KeyboardInterrupt:
                break
        
        # 통계 업데이트
        self.stats['rock_paper_scissors']['wins'] += wins
        self.stats['rock_paper_scissors']['losses'] += losses
        self.stats['rock_paper_scissors']['ties'] += ties
        self.save_stats()
    
    def number_guessing_game(self):
        """숫자맞추기 게임 (간소화된 버전)"""
        print("\n🔢 숫자맞추기 게임!")
        print("1부터 100까지의 숫자를 맞춰보세요!")
        
        secret = random.randint(1, 100)
        attempts = 0
        max_attempts = 7
        
        while attempts < max_attempts:
            try:
                guess = input(f"\n추측 ({attempts + 1}/{max_attempts}): ").strip()
                
                if guess.lower() == 'q':
                    break
                
                guess = int(guess)
                attempts += 1
                
                if guess == secret:
                    print(f"🎉 정답! {attempts}번 만에 맞추셨네요!")
                    self.stats['number_guessing']['wins'] += 1
                    if attempts < self.stats['number_guessing']['best_attempts']:
                        self.stats['number_guessing']['best_attempts'] = attempts
                        print("🏆 최고 기록 갱신!")
                    self.save_stats()
                    return
                elif guess < secret:
                    print("📈 더 큰 숫자입니다!")
                else:
                    print("📉 더 작은 숫자입니다!")
                    
            except ValueError:
                print("❌ 숫자를 입력해주세요!")
            except KeyboardInterrupt:
                break
        
        print(f"💥 실패! 정답은 {secret}이었습니다.")
        self.stats['number_guessing']['losses'] += 1
        self.save_stats()
    
    def show_stats(self):
        """게임 기록 표시"""
        print("\n📊 게임 기록")
        print("="*30)
        
        rps = self.stats['rock_paper_scissors']
        total_rps = rps['wins'] + rps['losses'] + rps['ties']
        
        print("🪨📄✂️ 가위바위보:")
        if total_rps > 0:
            win_rate = (rps['wins'] / total_rps) * 100
            print(f"  총 {total_rps}게임 | {rps['wins']}승 {rps['losses']}패 {rps['ties']}무")
            print(f"  승률: {win_rate:.1f}%")
        else:
            print("  아직 플레이 기록이 없습니다.")
        
        print("\n🔢 숫자맞추기:")
        ng = self.stats['number_guessing']
        total_ng = ng['wins'] + ng['losses']
        
        if total_ng > 0:
            success_rate = (ng['wins'] / total_ng) * 100
            print(f"  총 {total_ng}게임 | {ng['wins']}승 {ng['losses']}패")
            print(f"  성공률: {success_rate:.1f}%")
            if ng['best_attempts'] < 999:
                print(f"  최고 기록: {ng['best_attempts']}번만에 성공!")
        else:
            print("  아직 플레이 기록이 없습니다.")
    
    def show_challenges(self):
        """도전 과제 표시"""
        print("\n🏆 도전 과제")
        print("="*30)
        
        rps = self.stats['rock_paper_scissors']
        ng = self.stats['number_guessing']
        
        challenges = [
            ("🔥 가위바위보 첫 승리", rps['wins'] >= 1),
            ("💪 가위바위보 10승 달성", rps['wins'] >= 10),
            ("🎯 숫자맞추기 첫 성공", ng['wins'] >= 1),
            ("🧠 숫자맞추기 3번만에 성공", ng['best_attempts'] <= 3),
            ("👑 숫자맞추기 5번 연속 성공", ng['wins'] >= 5)
        ]
        
        for challenge, completed in challenges:
            status = "✅" if completed else "⭕"
            print(f"  {status} {challenge}")
    
    def show_help(self):
        """도움말 표시"""
        print("\n❓ 도움말")
        print("="*30)
        print("🪨📄✂️ 가위바위보:")
        print("  - 컴퓨터와 가위바위보 대결!")
        print("  - 가위는 보를 이기고, 바위는 가위를 이기고, 보는 바위를 이깁니다.")
        print("\n🔢 숫자맞추기:")
        print("  - 1-100 사이의 숫자를 맞춰보세요!")
        print("  - 7번의 기회가 주어집니다.")
        print("  - 힌트를 참고해서 정답을 찾아보세요!")
        print("\n💡 팁:")
        print("  - 모든 게임에서 'q'를 입력하면 메인 메뉴로 돌아갑니다.")
        print("  - 게임 기록은 자동으로 저장됩니다.")
    
    def run(self):
        """게임센터 실행"""
        print("🎮 미니 게임센터를 시작합니다!")
        
        while True:
            try:
                self.show_main_menu()
                choice = input("메뉴를 선택하세요: ").strip()
                
                if choice == '1':
                    self.rock_paper_scissors_game()
                elif choice == '2':
                    self.number_guessing_game()
                elif choice == '3':
                    self.show_stats()
                elif choice == '4':
                    self.show_challenges()
                elif choice == '5':
                    self.show_help()
                elif choice == '6':
                    print("🎮 미니 게임센터를 이용해주셔서 감사합니다!")
                    print("👋 다음에 또 만나요!")
                    break
                else:
                    print("❌ 1-6 사이의 숫자를 선택해주세요!")
                    
            except KeyboardInterrupt:
                print("\n\n👋 게임을 종료합니다.")
                break

# 게임센터 실행
if __name__ == "__main__":
    game_center = GameCenter()
    game_center.run()
```

## 🎯 완성도 높이기

### Chat에 이런 질문들을 해보세요

**1. 게임 개선:**

```
"이 게임들에 어떤 기능을 더 추가하면 재미있을까요?"
"난이도 조절 기능을 추가하는 방법을 알려주세요."
```

**2. 코드 최적화:**

```
"이 코드에서 중복되는 부분을 줄이는 방법이 있나요?"
"더 효율적인 코드 구조로 개선해주세요."
```

**3. 새로운 게임:**

```
"비슷한 난이도로 만들 수 있는 다른 게임 아이디어를 제안해주세요."
"틱택토 게임을 추가하려면 어떻게 해야 하나요?"
```

## 🎊 학습 성과 체크

### 오늘의 달성도

```
✅ 완료 체크:
- [ ] 게임 로직 설계 및 구현
- [ ] 사용자 상호작용 시스템 구축
- [ ] 에러 처리가 포함된 견고한 프로그램
- [ ] 파일 I/O를 활용한 데이터 저장
- [ ] 객체지향 프로그래밍 기초 적용
- [ ] 완전한 미니 프로젝트 완성
```

### 실력 향상 지표

```
Before (Chapter 2-3 이후):
- 개별 개념들을 따로 이해
- 간단한 예제 코드 작성 가능

After (Chapter 2-4 이후):
- 여러 개념을 통합한 완전한 프로그램 설계
- 사용자 경험을 고려한 인터페이스 구현
- 실제 사용 가능한 수준의 프로젝트 완성
- 게임 개발의 기초 원리 이해
```

## 🌟 마무리

축하합니다! 이제 여러분은 **진짜 게임 개발자**가 되었습니다! 🎉

### 🔥 오늘의 성취

1. **두 개의 완전한 게임 완성**
2. **통합 게임센터 시스템 구축**
3. **모든 배운 개념들의 실전 적용**
4. **사용자 친화적 인터페이스 설계**

### 💬 다음 Chapter 예고

다음 Chapter 3에서는 **Edit 기능으로 코드 개선하기**를 배워보겠습니다!

- Ctrl+K로 코드 선택하고 개선하기
- AI와 함께 코드 리팩토링하기
- 더 읽기 쉽고 효율적인 코드 만들기

### 🎯 과제 (선택사항)

**게임 개발자 도전:**

```
AI에게 요청:
"오늘 만든 미니 게임센터에 새로운 게임을 하나 더 추가해주세요.
기존 게임들과 비슷한 수준의 복잡도로 만들어주세요."
```

**여러분은 이제 AI와 함께 어떤 프로그램이든 만들 수 있는 진짜 개발자가 되었습니다!** 🚀

---

*💡 오늘 만든 게임들을 친구들에게 자랑해보세요! 여러분이 직접 만든 첫 번째 게임입니다!*
